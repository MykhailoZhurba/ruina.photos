---
/** /lv/, /ru/, /uk/ — the same page as the English root, in another language. */
import HomePage from '../../views/HomePage.astro';
import { PREFIXED_LOCALES } from '../../i18n/locales';

export const getStaticPaths = () => PREFIXED_LOCALES.map((locale) => ({ params: { locale } }));
---

<HomePage />
